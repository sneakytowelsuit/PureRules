---
import Navigation from "../components/Navigation.astro";
const pageTitle: string = 'About PureRules';
---
<head>
  <meta charset="utf-8"/>
  <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`}/>
  <meta name="viewport" content="width=device-width"/>
  <meta name="generator" content={Astro.generator}/>
  <title>{pageTitle}</title>
  <style>
    :root { --bg:#f6f5f1; --fg:#111; --border:4px; --shadow:6px; }
    body { margin:0; background: var(--bg); color: var(--fg); font:16px/1.5 ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Inter, Arial, sans-serif; }
    .wrap{max-width:900px;margin:40px auto;padding:20px}
    .card{background:#fff;border:var(--border) solid #111;box-shadow:var(--shadow) var(--shadow) 0 #111;padding:24px}
    a.btn{padding:8px 10px;background:#fff;border:var(--border) solid #111;box-shadow:var(--shadow) var(--shadow) 0 #111;font-weight:800}
  </style>
</head>
<body>
<Navigation />
  <div class="wrap">
    <div class="card">
      <h1>{pageTitle}</h1>
      <p>PureRules is built for clarity, composability, and speed. It supports deterministic
      and probabilistic evaluation modes, and a small set of extensible operators.</p>
      <p><a class="btn" href={`${import.meta.env.BASE_URL}/PureRules/docs/`}>Read the docs</a></p>
    </div>
  </div>
</body>